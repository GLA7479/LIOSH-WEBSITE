# `apps/`

The **`leok-games-shell/`** subtree is a **deprecated Capacitor** experiment and **not** part of the browser-installed PWA product. See `leok-games-shell/DEPRECATED-NOT-WEB-PRODUCT.md`.

When file locks are released, delete `leok-games-shell` entirely so the repo root stays web-only.
