import { useEffect } from "react";

// Minimal placeholder so existing game pages work.
// You can replace this with your full implementation later.
export function useIOSViewportFix() {
  useEffect(() => {
    // no-op for now
  }, []);
}
